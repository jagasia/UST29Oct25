Java
----
Java as a langauge, is Object oriented programming language.
Java as a platform.
Java is platform independent.
	it does not mean all platforms.
	Java runs in 3 OS:
		Linux	/ android
		Windows
		MacOs


Java as a language, has 3 editions:
	Java Standard edition		(core java)
	Java Enterprise edition		(adv java)
	Java Mobile edition		(for mobile)

We are in Java SE, core java.

I learned 
		java jdk 1.1
1.2
1.3
1.4

upto the above versions, java was called as 
	J2SE
	J2EE
	J2ME

from version 1.5 onwards

	Java SE 5
	Java EE 5


6
7
8
11
17
21
23

These were the LTS versions

Long Term Support

JDK
	java development kit
		this is for the developers, to code, test

JRE
	java runtime environment
		this is for users, running the code


JVM
	JRE contains JVM. When we run the code, JVM will
		use JIT compiler to further compile the byte code into machine specific code and run.


Java compilation process:
	our java code is translated into byte code by javac
	while running the code, it is further translated from byte code into machine language code using JIT of JVM of JRE







Microsoft Windows [Version 10.0.19045.6456]
(c) Microsoft Corporation. All rights reserved.

D:\Jag\UST\Batch_29Oct25\all\Java>javac MyJavaApp.java

D:\Jag\UST\Batch_29Oct25\all\Java>java MyJavaApp
Hello from MyJavaApp!
The sum of 10 and 25 is: 35

D:\Jag\UST\Batch_29Oct25\all\Java>




   *   Which of the three components (JVM, JRE, JDK) was primarily responsible for allowing you to use the `javac` command to compile your `.java` file into a `.class` file? Explain why.
JDK provides everything. Including javac 

        *   Which of the three components (JVM, JRE, JDK) was primarily responsible for allowing you to use the `java` command to execute your `.class` file and see its output? Explain why.
        *   Briefly describe the core role of each: JVM, JRE, and JDK, connecting them to your experience in this task.



The class name must follow PascalCase.
	every first letter is in upper case only
	ex:
		MyJavaApp.java

The method names and variables, must be in camelcase.
	the first letter is in camel case. But subsequent words first letter will be in Upper case.
	ex:
		float calculateSimpleInterest(){}
		int maximumScore;



Lets start using IDE	(Integrated Development Environment)
	allows us to type the code
	save the code
	helps us in getting "intellisense"
		auto completion
		suggestions, references
	compile as you type itself and show syntax errors 
	debugging
	run the code
	package the code as jar/war/ear
	allow us to install plugins to get various supports.


Lets use Spring Tool Suite

when i open sts, it asks me to choose a workspace. 
	here, you choose a folder where all your projects will be 		created.
		you can choose a location, and it is not there, it 			will create for you.


Lets create a project now in STS.

1) File -> New -> Java Project

if this option is not present, then choose "Project" option
	file -> new -> Project
		then search for "Java Project" option.

	enter the name of the project "29-oct-2"

A folder will be created in your workspace as your project name.



What is debugging?
	finding the bug, usually a logical error in a program.




1) Break point
	choose a line of code, from where you need to start debugging.
2) Run the coding in debug mode.
3) When the code is in break, in debug perspective,
	we have 
		step into
			move to next line
		step over
			move to next statement
		step return/out
			complete the current function and exit


	
----------------------------------------
Understanding Classpath and Java Packages


Can you create a class whose name is 'Scanner'?
	Yes. we can create a class called canner.
	The confusion you have is there is already a Scanner class available in java.util package.

	java.util.Scanner sc=new java.util.Scanner(System.in);
	the above is unusual, because, we normally import 

import java.util.Scanner;

	now if i say Scanner, then it means java.util.Scanner only.

class Employee{
}

Employee raja=new Employee();


in java, a package will be a folder for sure.

------------------
Command line arguments
------------------------
javac App.java
java App


java App Raja
	Hello Raja



