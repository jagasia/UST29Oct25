Spring cloud
	eureka server and client		service discovery and monitoring
	feign client				accessing other services
	api gateway			uniform interface
	load balancer	
	circuit breaker
	configuration

Spring security
	entity
	repository
	service


	security filter chain
		url is authenticated
		url is permitted without authentication



	AuthenticationManager
		authenticate()	method
	




http://localhost:8080/product


http://localhost:8080/api/v2/product


http://localhost:8080/api/v1/login






/api/v1/**

-------------------------------------
Spring cloud
------------------

Spring cloud config server

Demo:

we will create a folder in our computer


1) we have created a spring boot app now, 
		spring web
		spring cloud cloud config server


2) in main class:

enable config server

@EnableConfigServer


3) in application.properties:

spring.application.name=config-server

server.port=8888
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=file:/D:/Jag/UST/Batch_29Oct25/all/niranj



===============
create product-service project


	spring web
	spring cloud cloud config client

-------------------------


1) create a folder or git repository.
keep the configuration files here.
Naming convention:
	application.properties
	{application name}-{active profile}.properties

2) create a spring cloud config server.
	this is a spring boot project only.
		spring web
		spring cloud - config server

3) enable config server

4) application properties:

server.port=8888

spring.application.name=config-server
spring.profiles.active=native

spring.cloud.config.server.native.searchLocations=file:/D:/jag/ust/Batch_29Oct25/all/todelc



then run the server.
native	=	local file system
if spring.profiles.active is not set, it is default to git.
But this is only in spring cloud config server. 

in services, dev or prod or whatever we choose, will be used to choose from the corresponding configuration file using
naming convention. Ex: order-service-dev.properties if 
spring.application.name=order-service
spring.profiles.active=dev

what is a Spring profile?

A profile is just a mode in which a Spring application runs.

Examples:

dev
test
prod
native
A profile activates certain beans and configuration paths.
==================================================




complete notes:


		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-config-server</artifactId>
		</dependency>


server.port=8888

spring.application.name=config-server
spring.profiles.active=native

spring.cloud.config.server.native.searchLocations=file:/D:/jag/ust/Batch_29Oct25/all/todelc





@EnableConfigServer

----
End of config server part.
-------------------------------------------------------------------------------
now in one of services.

order-service.

		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-config</artifactId>
		</dependency>


server.port=8081
spring.application.name=order-service
spring.profiles.active=dev

# This tells Spring Boot to import config from the Config Server
spring.config.import=optional:configserver:http://localhost:8888




package com.ust.demo;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MessageController {

  @Value("${app.message:missing}")
  private String message;

  @GetMapping("/message")
  public String message() {
    return message;
  }
}






==================================================================
Eureka server and clients
------------------------------------

1) eureka server project
2) many eureka client projects




eureka server project steps:


1) create a spring boot app

	eureka-server

		spring web
		spring cloud eureka server

	enable eureka server

application.properties
	eureka.client.registerWithEureka = false
	eureka.client.fetchRegistry = false
	server.port = 8761



run the project


http://localhost:8761

------------------------
in each webservice, we will register to that eureka server.



<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
</dependency>



in application.properties

eureka.instance.hostname=localhost
eureka.client.serviceUrl.defaultZone=http://${eureka.instance.hostname}:8761/eureka/
server.context-path=/product






YAML
	YAML AREN'T MARKUP LANGUAGE


