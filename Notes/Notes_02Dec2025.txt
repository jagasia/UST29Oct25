JPA
-----
spring web
spring data jpa
mysql driver


JPAQL



id
name
category
price


findByName
findByCategory
findByPrice

for above methods, there is no need to write queries. Instead, we can use finder methods


package com.ust.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import com.ust.demo.model.Product;
import java.util.List;


@Repository
public interface ProductRepository extends JpaRepository<Product, Integer> {

	List<Product> findByCategory(String category);
}







find all products in the range of x and y






package com.ust.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import com.ust.demo.model.Product;
import java.util.List;


@Repository
public interface ProductRepository extends JpaRepository<Product, Integer> {

	List<Product> findByCategory(String category);
	
//	@Query(value="select p from Product p where p.price between :min and :max", nativeQuery = false)
	@Query("select p from Product p where p.price between :min and :max")
	List<Product> findByPriceRange(@Param("min") Double min,@Param("max") Double max);
}



ResponseEntity


JPA Relationships
------------------------
1 to 1
1 to Many
Many to 1
Many to Many



Customer Purchase Product


Customer	Account


1 to Many


1	Department	has	many	Employee






CUSTOMER
PRODUCT
SALES
	CID
	PID


