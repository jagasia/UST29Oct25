Comparable makes objects comparable.
Only if we can compare objects, they can be sorted.



Stream API
	Stream means cascading
	is similar to SQL SELECT statement
	does not affect the original collection
	

there are 2 types of operations
	intermediate 
	terminal 


intermediate operations returns another stream
	filter
	sort
	map

terminal operation can be
	forEach
	collect





Functional Interface
	an interface that has only 1 abstract method

Marker Interface
	an interface that has no abstract method
	ex:
		Serializable


Functional Interface
Lambda
Method Reference
Stream






import java.util.Arrays;
import java.util.Collections;
import java.util.Comparator;
import java.util.List;

class NumDescComparator implements Comparator<Integer>{

	@Override
	public int compare(Integer o1, Integer o2) {
		return o2.compareTo(o1);
	}
	
}

public class App {

	public static void main(String[] args) {
		List<Integer> nums=Arrays.asList(11,1,22,2,5,3,4);
//		Collections.sort(nums,new NumDescComparator());
		Collections.sort(nums, (x,y)-> y.compareTo(x));
		System.out.println(nums);
	}

}







System.out::println





public class Employee {
    private Long id;
    private String name;
    private String department;
    private Double salary;

    public Employee() {
    }

    public Employee(Long id, String name, String department, Double salary) {
        this.id = id;
        this.name = name;
        this.department = department;
        this.salary = salary;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getDepartment() {
        return department;
    }

    public void setDepartment(String department) {
        this.department = department;
    }

    public Double getSalary() {
        return salary;
    }

    public void setSalary(Double salary) {
        this.salary = salary;
    }

    @Override
    public String toString() {
        return "Employee [id=" + id + 
               ", name=" + name + 
               ", department=" + department + 
               ", salary=" + salary + "]";
    }
}
-----------------------------
Thread
----------
	Light weight process
	Process is heavy weight

	What is the relationship between process and thread?
		Process can contain threads
		Thread is a part of process


	What is a process?
		is a running instance of a program.



CPU allocates resources to each process.
	time 		CPU time		2.8 * 1024 * 1024 * 1024 
	memory


Performance
	effective utilization of resources


Every process has at least 1 thread 
	that thread is called as "main" thread


	any threads we create, are child threads only




1		0%
2		50%
3		66%
4		75%

Optimal use of multi-threading have impact on performance

before multi core 

	concurrency

now
	parallelism


Lets create threads.

	1) implements Runnable
	2) extends Thread



class Two implements Runnable{
	public void run(){
		for(int i=0;;i++){
			System.out.println(Thread.currentThread().getName()+"\t"+i);
		}
	}
}


--------------------------------------------------------------------------
Threads



class MyThread extends Thread{
	public void run(){
	}
}


Life cycle states
------------
New Thread state			when you create an object of Thread class
Runnable				when you start the thread
Not Runnable			when waiting, sleeping, blocked
Dead				i) when the work is complete ii) the process that contains the thread is dead, iii) null is assiged to the thread



Thread priorities
	NORMAL		5
		1 to 10
		
Monitor

Synchronization
----------------------




public class App {

	public static void main(String[] args) throws InterruptedException {
		FibanocciGenerator t1=new FibanocciGenerator("Thread 1", 20);
		FibanocciGenerator t2=new FibanocciGenerator("Thread 2", 20);
		FibanocciGenerator t3=new FibanocciGenerator("Thread 3", 10);
		FibanocciGenerator t4=new FibanocciGenerator("Thread 4", 10);
		
		t1.start();
		t2.start();
//		t2.join();			//
		t3.start();
		t3.join(); 			//
		t4.start();
	}

}








synchronized block
synchronized method
volatile	int i;				//thread will not cache them. Always access from main memory

